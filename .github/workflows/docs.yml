name: docs

on:
  pull_request:
    paths:
      - "docs/**"
      - "README.md"
      - "scripts/ci/check_ascii.py"
      - "scripts/ci/test_check_ascii.py"
      - "scripts/ci/check_runbook_anchors.py"
      - "tests/ci/test_check_runbook_anchors.py"
      - ".github/workflows/docs.yml"
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ascii:
    name: ascii punctuation
    # AGENTS.md: workflows run on ubuntu-latest only.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Test the ASCII check
        run: python3 -m unittest scripts/ci/test_check_ascii.py -v

      - name: Check ASCII punctuation across docs
        run: python3 scripts/ci/check_ascii.py docs

  runbook-anchors:
    name: runbook anchors
    # AGENTS.md: workflows run on ubuntu-latest only.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Test the runbook-anchor check
        run: python3 -m unittest tests/ci/test_check_runbook_anchors.py -v

      - name: Check runbook anchors
        run: python3 scripts/ci/check_runbook_anchors.py

  diagram-renders:
    name: diagram renders
    # AGENTS.md: workflows run on ubuntu-latest only.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Render Draw.io diagrams
        shell: bash
        run: |
          mkdir -p "$RUNNER_TEMP/drawio-home" "$RUNNER_TEMP/diagram-renders"

          docker run --rm \
            -u "$(id -u):$(id -g)" \
            -e HOME=/home/drawio \
            -v /etc/passwd:/etc/passwd:ro \
            -v "$RUNNER_TEMP/drawio-home:/home/drawio" \
            -v "$GITHUB_WORKSPACE:/data:ro" \
            -v "$RUNNER_TEMP/diagram-renders:/output" \
            -w /data \
            rlespinasse/drawio-desktop-headless@sha256:b7fe022af559d73240111aaa97b07c516d2201c4837f73f0d5c36d6568ca941e \
            -x -f svg --embed-svg-images \
            -o /output/azure-architecture.svg \
            docs/diagrams/azure-architecture.drawio

          docker run --rm \
            -u "$(id -u):$(id -g)" \
            -e HOME=/home/drawio \
            -v /etc/passwd:/etc/passwd:ro \
            -v "$RUNNER_TEMP/drawio-home:/home/drawio" \
            -v "$GITHUB_WORKSPACE:/data:ro" \
            -v "$RUNNER_TEMP/diagram-renders:/output" \
            -w /data \
            rlespinasse/drawio-desktop-headless@sha256:b7fe022af559d73240111aaa97b07c516d2201c4837f73f0d5c36d6568ca941e \
            -x -f png \
            -o /output/azure-architecture.png \
            docs/diagrams/azure-architecture.drawio

      - name: Check committed renders are current
        shell: bash
        run: |
          sed -E 's/ge-svg-[A-Za-z0-9_-]+/ge-svg-deterministic/g' \
            docs/diagrams/azure-architecture.svg > "$RUNNER_TEMP/azure-architecture.committed.svg"
          sed -E 's/ge-svg-[A-Za-z0-9_-]+/ge-svg-deterministic/g' \
            "$RUNNER_TEMP/diagram-renders/azure-architecture.svg" > "$RUNNER_TEMP/azure-architecture.generated.svg"
          cmp -- "$RUNNER_TEMP/azure-architecture.committed.svg" "$RUNNER_TEMP/azure-architecture.generated.svg"
          cmp -- docs/diagrams/azure-architecture.png "$RUNNER_TEMP/diagram-renders/azure-architecture.png"
