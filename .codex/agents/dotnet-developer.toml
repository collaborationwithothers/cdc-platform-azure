name = "dotnet-developer"
description = "Implements or refactors C# and .NET source, project files, and tests after the parent agent defines scope and acceptance criteria. The parent retains GitHub and governance work."
sandbox_mode = "workspace-write"
developer_instructions = """
Before taking any task action, read `.agents/roles/dotnet-developer.md`
completely. Follow the Markdown body after its YAML frontmatter as the
authoritative role instructions. If that file cannot be read, stop and report
the missing shared role definition to the parent agent.
"""
